# k8s/proxy/proxy-deployment.yaml

apiVersion: apps/v1
kind: Deployment
metadata:
  name: reverse-proxy
  namespace: lovable-core
spec:
  replicas: 1
  selector:
    matchLabels:
      app: reverse-proxy
  template:
    metadata:
      labels:
        app: reverse-proxy
    spec:
      containers:
        - name: reverse-proxy
          image: nagrajh/lovable-reverse-proxy:0.0.1
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "250m"
              memory: "512Mi"
          envFrom:
            - configMapRef:
                name: lovable-shared-config
          env:
            - name: REDIS_URL
              value: "redis://redis-0.redis.lovable-core.svc.cluster.local:6379"
            - name: PORT
              value: "80"
---
apiVersion: v1
kind: Service
metadata:
  name: reverse-proxy
  namespace: lovable-core
spec:
  selector:
    app: reverse-proxy
  ports:
    - port: 80
      targetPort: 80